prereqs:
  runtime:
    requires:
      File::Which:
      Getopt::Long: { version: '2.39' }
      HTTP::Tiny: { version: '0.082' }
      IO::Socket::SSL:
      Term::ReadKey:
      Text::Fold:
      perl: { version: 'v5.16' }
  develop:
    requires:
      Capture::Tiny:
